# PyTorch Training Parameters
model:
  backbone_arch: dinov2_vitb14
  backbone_config: 
    num_trainable_blocks: 4
    return_token: True
    norm_layer: True
    domain_prompt: False
    num_clusters: 64
    cluster_dim: 16
    token_dim: 256
    dropout: 0.3
    padding: "detach"
    divide: 1
    decouple: False
    shared_clusters: 0
    freeze_backbone: False
  agg_arch: SALAD
  agg_config:
    num_channels: 768
    num_clusters: 64
    cluster_dim: 128
    token_dim: 256
    dropout: 0.3
    padding: "detach"
    divide: 1
    decouple: False
    shared_clusters: 0

datasets:
  train_datasets: 
    - mapillary_sls
  val_datasets: 
    - pitts30k
    - pitts250k
    - mapillary_sls
    - svox
    - SF_XL
  test_datasets:
    - pitts30k
    - pitts250k
    - mapillary_sls
    - svox
    - tokyo247
    - SPED
    - nordland
    - nordland_subset
    - amstertime
    - eynsham
  target_val_dataset: pitts30k
training:
  num_epochs: 64
  train_batch_size: 30
  test_batch_size: 256
  num_workers: 8
  train_image_size: [224, 224]
  test_image_size: [322, 322]
  optimizer:
    name: AdamW
    lr: 6e-5
    weight_decay: 9.5e-9
    momentum: 0.9
  scheduler:
    name: linear
    args:
      start_factor: 1
      end_factor: 0.2
      total_iters: 4000
  loss:
    name: 'MultiSimilarityLoss'
  miner:
    name: 'MultiSimilarityMiner'
    margin: 0.1
  faiss_gpu: False
  only_top_k: True
  recompute_clusters: False
  recompute_interval: 4
  shuffle_method: global
  prefetch_factor: 1